# Springboot-TaskManager
 A  task manager application built using Spring Boot for the backend and React for the frontend. The application supports basic CRUD operations (Create, Read, Update, Delete) for managing tasks.
